<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Futura Font Example</title>
    <style>
        /* Add @font-face rules here if using local fonts */
        
        .header {
            font-family: 'Futura', Arial, sans-serif;
            background-color: #ADD8E6; /* Light blue background */
            padding: 10px; /* Adds some space around the text */
            text-align: center; /* Centers the text horizontally */
        }
        
        .header h1 {
            font-weight: bold; /* Makes the font bold */
            color: #000; /* Black text color */
        }
        
        .taco-list {
            font-family: 'Futura', Arial, sans-serif;
            padding: 20px; /* Adds some space around the list */
            list-style-type: none; /* Removes default bullet points */
        }
        
        .taco-list li {
            margin-bottom: 10px; /* Adds space between list items */
        }
        
        .taco-list li::before {
            content: "\25B8"; /* Adds a custom bullet point */
            margin-right: 10px; /* Space between bullet and text */
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Jonathon's First Webpage</h1>
    </div>
    
    <ul class="taco-list">
        <li>Chicken</li>
        <li>Beef</li>
        <li>Carnitas</li>
        <li>Hongo (Mushroom)</li>
    </ul>
</body>
</html>

<% tacos = ["carnitas", "al pastor", "pescado"] %>

<p>We hace <%= tacos.length %> tacos:</p>

<ul>
  <li>Carnitas</li>
<ul>
